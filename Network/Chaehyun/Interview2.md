Q : 로그인 처리를 할 떄 세션 기반 인증, 토큰 기반 인증의 차이점?

모릅니당

---

Q : TCP와 UDP의 차이점에 대해서 알고계씰것 같은데 어떤 서비스 환경이 적합할지 예시를 들어주세요

신뢰성과 빠른 속도 면에서 차이가 있다

TCP - 꼭 응답을 확인해야함 꼭 데이터가 변경되었을때 ~~ 은행에서도 송금같은 중요한 로직에서 TCP를 사용할 것 같음

UDP - 내용이 사라지더라도 빠르게 이루어지는 경우, 유튜브같은 스트리밍

---

Q : https 통신 과정에서 대칭키-비대칭키를 혼용해서 사용하는데 그 이유?

이유는 잘 모르지만

https는 http에 ssl이 붙는 방식임

서버와 클라이언트 사이에서 암호화, 복호화를 하는데 ca 인증서를 확인하는 과정에서 클라이언트가 대칭키를 발급받고 그것을 암호화해서 넘기면 서버가 자신의 키로 복호화하여 인증하는 것

---

Q : 개발하면서 cors 겪어본 경험?

프론트엔드 url 주소를 서버 개발자가 cors에 오류가 나지 않도록 허용을 해주지않아서 오류가 난 적 있음

해결방안?

로컬호스트 포트번호를 같이 넘겨주거나 배포한 도메인 사이트를 넘겨주면 요청을 받을 때 이 origin은 허용을 해줘~ 이렇게 코드를 작성하면 그 부분이 우회가 되는 식으로 해결함

---

Q : http와 웹 소켓의 차이점 ?

http는 단방향으로 소통을 할 수 있어서 실시간으로 알림이 발생할때 처리가 어려움

폴링 방식으로 해야하는데 과정이 복잡함

웹 소켓은 양방향 소통 가능함

---

Q : 소켓과 웹 소켓의 차이점 ?

모르겠습니다

---

Q : tcp를 사용할 때 패킷의 순서를 보장 어떻게?

헤더를 사용하는데 데이터를 보낼 때 시퀀스에 대한 정보를 보내주고 그것을 보내면 수신측에서 그 패킷이 쪼개져서 오더라도 그 순서에 맞게 재조합하고 맞게 오면 응답을 보내서 신뢰성을 보장함

---

Q : 소켓 통신에는 두가지 종류가 있는데, 개발자 입장에서 소켓을 생성할때 차이점?

모르겠습니다

---

Q : http 헤더에는 최초에 어떤 정보가 담겨있나요?

모르겠습니다

---

Q : http는 비연결성 프로토콜인데, 실제 서비스에서 매 요청마다 새로 연결할 때 오버헤드가 발생할 수 있는데 해결방안을 예시를 들어 설명해주세요

캐시를 이용하여 처리할 수 있다 정확히는 모르겠다

---

Q : tcp의 혼잡제어와 흐름제어의 차이점?

정상적으로 처리되지 않을때 처리를 위한 기능

큐에 처리가 다 되지 않을때

수신-송신의 속도가 맞지 않을 때 

---

Q : 네트워크 계층 과 다른 계층 과 연결하여 설명

전송 계층은 프로세스 간 통신 

각 호스트- 호스트가 연결되어야함

전송 계층은 세그먼트를 사용하여서 그 부분에 네트워크 끼리 소통할 수 있도록 정보를 추가해주어서 통신을 하도록 만드는 것으로 알고있습니다.




# Feedback

**남소미**

답변 내용은 좋았는데 자신감을 가지면 좋을 것 같습니다! 답변하기 전에 조금 시간이 걸려도 전체 내용을 대충이라도 머릿속에서 정리 후에 답변하면 더 좋을 것 같아용

**권나연**

말투가 상냥해서 좋습니다. 네트워크에 대한 지식도 해박한 것 같습니다. 근데 시선 처리를 더 잘하면 좋을 것 같습니다. 면접관 님을 바라보던가 번갈아가면서 보는 연습을 하면 좋을 것 같습니다.  그리고 면접관님이 해당 내용에 대해서 유식하지 않을 수도 있기 때문에 조금 더 비유를 들어서 알기 쉽게 설명하면 좋을 것 같습니다.
